# Platform.sh environment variables template
# Copy to .env for local development

APP_ENV=prod
APP_SECRET=CHANGE_THIS_SECRET_FOR_PRODUCTION

# Database URL will be auto-configured by Platform.sh
# DATABASE_URL=

# Redis URL will be auto-configured by Platform.sh  
# REDIS_URL=

# JWT Configuration
JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
JWT_PASSPHRASE=your_jwt_passphrase

# Encryption
ENCRYPTION_KEY=32_character_random_encryption_key

# Mailer (configure with your preferred service)
MAILER_DSN=smtp://localhost

# CORS (adjust for your domain)
CORS_ALLOW_ORIGIN=^https?://(localhost|127\.0\.0\.1)(:[0-9]+)?$